using UnityEngine;

namespace Supinfo.Project.Scripts
{
    public class UpgradeManager : MonoBehaviour
    {
        // for each event have a function to upgrade a specific stat
        // or generalize to have a function that can change any SOs
        
        // needs to upgrades health, attack (damage) and range
        // unit's health (3) / attack (4) (no range health upgrade)
        // unit range (for range attacker)
        // turret range and attack
        
    }
}